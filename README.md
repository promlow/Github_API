*clone.sh* is a BASH script to clone all of the repos that belong to a user.

```console
./clone.sh promlow
```

Will get all of my repos. For users with many (>100) repos, the script will paginate to collect them all. Tested with 136 repos.
